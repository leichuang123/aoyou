
// 用于定义数组

export interface ProductTimeCardItem {
  vmType: number,
  cardName: string,
  vmid: number,
  id: number
}

export interface PlaceItem {
  dictionarykey: number,
  dictionaryvalue: string,
  id: number,
  status: number,
  typeid: number
}